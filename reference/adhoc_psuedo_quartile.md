# ad-hoc psuedo quartile

can be used to get becca plot to return predicted performance bands,
instead of quartile bands

## Usage

``` r
adhoc_psuedo_quartile(x, breaks)
```

## Arguments

- x:

  vector to compare (presumably percentile ranks)

- breaks:

  vector of breaks

## Value

vector of 'quartiles'
