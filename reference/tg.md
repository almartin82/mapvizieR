# tg "to grob" helper function

tg "to grob" helper function

## Usage

``` r
tg(x)
```

## Arguments

- x:

  a report object. currently supports grobs or ggplot objects

## Value

a grob, suitable for arrangeGrob on a report layout
