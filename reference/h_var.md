# h_var

utility function to make a text grob of desired size

## Usage

``` r
h_var(text, size, gp = grid::gpar(fontsize = size, fontface = "bold"))
```

## Arguments

- text:

  text you want on the grob

- size:

  font size

- gp:

  graphics paramaters.
