# ensure_fields

a simple validation function to check for necessary fields used by a
function

## Usage

``` r
ensure_fields(fields_vector, df)
```

## Arguments

- fields_vector:

  vector of fields that your function needs.

- df:

  data frame that needs to have the fields
