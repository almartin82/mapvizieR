# ensure_goals_obj

a contract that ensures that a goal object's has the proper elements and
that the `goals` element data frame has columns names `accel_growth` and
`met_acc`

## Usage

``` r
ensure_goals_obj(x)
```

## Arguments

- x:

  object to check
